// Define o componente NotFound
function NotFound() {
  return <div>page doesn't exists</div>; // Retorna um div com o texto "page doesn't exists"
}

// Exporta o componente NotFound como padrão
export default NotFound;
